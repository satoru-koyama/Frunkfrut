// $(function(){
//   $('.post_index').hide();

//   $('.indexList').on('click',function(){
//     // クリックした要素の ID と違うクラス名のセクションを非表示
//     $('.post_index').not($('.'+$(this).attr('id'))).hide();
//     // クリックした要素の ID と同じクラスのセクションを表示
//     $('.'+$(this).attr('id')).show();

//     // toggle にすると、同じボタンを 2 回押すと非表示になる
//     // $('.'+$(this).attr('id')).toggle();
//   });
// });